# 🔌 دليل الربط الكامل — لكل مصدر ترافيك محتاج إيه

هذا الدليل يشرح **عملياً** كيفية ربط كل مصدر يصل منه العملاء إلى نظام Nexus CRM، وما المطلوب بالضبط لكل مصدر، وما الذي يعمل مجاناً فوراً مقابل ما يحتاج إعداد من عندك.

> ⚠️ **قاعدة ذهبية:** الموقع مستضاف على GitHub Pages = **ملفات ثابتة فقط، بدون سيرفر خلفي**. لذلك أي ربط "مباشر" بواتساب/ماسنجر يتطلب وسيطاً مجانياً واحداً على الأقل (Google Apps Script أو Supabase). الوسيط هو سيرفر صغير مجاني يستقبل البيانات نيابةً عنك.

---

## جدول ملخص سريع

| مصدر التدفق | هل يعمل الآن؟ | المطلوب لتشغيله | المكان الذي تُعدّه |
|---|---|---|---|
| نموذج الموقع (اتصل بنا / راسلنا) | ✅ فوراً (يرسل عبر واتساب) | اختياري: رابط Google Apps Script | `integration/sheets-webhook.gs` |
| واتساب الشركة (استقبال عملاء) | ⚠️ يعمل بعد إعداد Meta | حساب WhatsApp Business + توكن + Webhook | `integration/whatsapp-webhook.gs` |
| فيسبوك ماسنجر صفحة مشكاة | ⚠️ يعمل بعد إعداد Meta | نفس تطبيق Meta + Page Token | نفس الملف أعلاه |
| مزامنة كل الموظفين (أجهزة متعددة) | ⚠️ بعد إعداد Supabase | قاعدة بيانات سحابية مجانية | `integration/supabase.sql` |
| التوزيع العادل على الموظفين | ✅ **يعمل الآن** | لا شيء | داخل CRM نفسه |
| تقرير الخطوات لحظة بلحظة | ✅ **يعمل الآن** | لا شيء | داخل CRM نفسه |
| فتح محادثة واتساب من ملف العميل | ✅ **يعمل الآن** | لا شيء | زر "فتح محادثة واتساب" |

---

## 1) نموذج الموقع "اتصل بنا / راسلنا" ← نظام CRM

### ماذا يوجد الآن؟
ملف `lead-capture.html` صفحة جاهزة تضع رابطها في موقع مشكاة. من دون أي إعداد:
- العميل يملأ البيانات → تُفتح محادثة واتساب مع رقم الشركة مباشرة (بياناته محفوظة في الرسالة).
- هذا **يعمل فوراً** بمجرد رفع الملف.

### لتشغيله تلقائياً بالكامل (البيانات تصل لجدول + تُوزَّع):
1. افتح [script.google.com](https://script.google.com) ← مشروع جديد.
2. الصق محتوى `integration/sheets-webhook.gs`.
3. شغّل `setupSheet` أول مرة (يطلب أذونات — وافق).
4. **نشر ← نشر كتطبيق ويب**:
   - تنفيذ كـ: **أنا**
   - الوصول: **أي شخص لديه الرابط**
   - انسخ رابط التطبيق (ينتهي بـ `/exec`).
5. ضع الرابط في ملف `lead-capture.html` (متغير `WEBHOOK_URL`):
   ```html
   var WEBHOOK_URL = 'https://script.google.com/macros/s/XXXX/exec';
   ```
6. اربط رابط الصفحة في موقع مشكاة (زر "اتصل بنا" أو "راسلنا").

### بعد كل إرسال:
- يظهر العميل في Google Sheet مع **التوزيع العادل** تلقائياً على الموظف الخمسة.
- المدير يستطيع استيراد الجدول في CRM (تصدير/استيراد) أو نسخ بيانات العميل.

---

## 2) ربط واتساب الشركة (استقبال عملاء تلقائياً + توزيع)

**الهدف:** أي عميل يبعت رسالة لرقم واتساب الشركة → يُنشأ ملف عميل → يُوزَّع على أقل موظف حملاً → الموظف يستقبل إشعار → العميل يستقبل رد تلقائي.

### المطلوب منك (مرة واحدة، مجاناً):
1. حساب **WhatsApp Business App** في [developers.facebook.com](https://developers.facebook.com).
2. إضافة منتج **WhatsApp** داخل التطبيق وربط رقم الشركة.
   - ملاحظة: الرقم الرئيسي للشركة يمكن توثيقه مجاناً، وإرسال الرسائل للموظفين والعملاء يتطلب حساب أعمال نشط.
3. إنشاء **System User Token** بصلاحية `whatsapp_business_messaging`.
4. من إعدادات WhatsApp → Webhook:
   - **Callback URL**: رابط التطبيق اللي هتنشره من `integration/whatsapp-webhook.gs`
   - **Verify token**: القيمة اللي في الملف `nexus_crm_verify_2026` (غيّرها لقيمة سرية عندك).
5. الاشتراك في حدث **messages**.

### خطوات النشر:
1. انسخ `integration/whatsapp-webhook.gs` إلى script.google.com.
2. عدّل `EMPLOYEES` و`EMPLOYEE_PHONES` بأسماء وأرقام موظفيك الحقيقية (بكري سليمان، هبة الله طاهر، مصطفى الشيخ، أحمد فؤاد، مريم هاني).
3. من **Project Settings ← Script Properties** ضع:
   - `WA_TOKEN` = توكن النظام
   - `WA_PHONE_ID` = معرف رقم الهاتف الموصول
   - `PAGE_TOKEN` و `PAGE_ID` = لماسنجر (القسم التالي)
4. انشر كتطبيق ويب (تنفيذ "أنا" / وصول "أي شخص").
5. ضع الرابط + الـ Verify Token في إعدادات Meta.
6. من تطبيق Meta اضغط "Verify and save" — سيعمل فوراً.

### النتيجة بعد الربط:
```
عميل → رسالة واتساب للشركة
   ↓
Webhook (Google Apps Script) يستقبل
   ↓
ينشئ عميلاً في Google Sheet + يوزّعه على أقل موظف حملاً
   ↓
يرسل إشعار واتساب للموظف + رد تلقائي للعميل
```

---

## 3) ربط ماسنجر صفحة مشكاة على فيسبوك

نفس الملف `integration/whatsapp-webhook.gs` يدعم ماسنجر — ما عليك سوى:
1. من نفس تطبيق Meta أضف منتج **Messenger** واربط صفحة مشكاة.
2. أنشئ **Page Access Token** بصلاحيات `pages_messaging`.
3. في إعدادات الصفحة ← Messenger ← Webhook ضع نفس الرابط + نفس Verify token.
4. اشترك في `messages` و `messaging_postbacks`.
5. ضع `PAGE_TOKEN` في Script Properties.

> عميل ماسنجر سينشأ عميله بنفس طريقة واتساب ويوزَّع بنفس العدالة.

---

## 4) مزامنة حقيقية لكل الموظفين عبر الأجهزة (Supabase)

المشكلة الحالية: كل جهاز يحفظ بياناته في متصفحه فقط. الحل الاحترافي المجاني: **Supabase** (قاعدة بيانات سحابية).

1. أنشئ مشروعاً على [supabase.com](https://supabase.com) (مجاني، بدون كارت).
2. من **SQL Editor** الصق محتوى `integration/supabase.sql` وشغّله — سينشئ الجداول + **دالة التوزيع العادل** التي تُسند أي عميل جديد تلقائياً.
3. من **Project Settings ← API** انسخ:
   - `Project URL`
   - `anon public key`
4. ضعهما في نظام CRM ← لوحة الإدمن ← **قسم الربط** ← Supabase.
5. من **Edge Functions** أنشئ دالة `webhook` والصق محتوى `integration/supabase-edge-function.js`، وأعد توجيه Webhook واتساب/ماسنجر إليها بدلاً من Apps Script إن أردت حلاً أكثر احترافية.

### بعدها:
- كل موظف يدخل النظام من أي جهاز يرى نفس البيانات لحظياً.
- التوزيع العادل يتم في قاعدة البيانات نفسها (بلا برمجة إضافية).
- يمكن ربط واتساب/ماسنجر بنفس الدالة.

---

## 5) التوزيع العادل → التوزيع بالكفاءة (تفعيل تدريجي)

من **لوحة الإدمن ← قسم الربط ← وضع التوزيع**:

- **العدالة (حالياً):** أي عميل جديد يُسند للموظف صاحب **أقل عدد عملاء مفتوحين** — عادل 100% من البداية.
- **الكفاءة (لاحقاً):** يُسند لمن يغلق صفقات أسرع، بحيث يُكافئ الأفضل أداءً مع الحفاظ على توازن الحمل.

غيّر الوضع متى شئت من القائمة المنسدلة واضغط حفظ.

---

## 6) أسئلة شائعة

**هل أستطيع ربط واتساب من GitHub Pages مباشرة بدون وسيط؟**
لا. GitHub Pages لا يقبل اتصالات واردة (webhooks). الوسيط المجاني (Google Apps Script أو Supabase) هو الحل المعتمد.

**هل أحتاج دومين خاص؟**
لا، كل الحلول المذكورة لا تحتاج دومين.

**هل التكلفة صفر؟**
نعم — Google Apps Script وSupabase المجاني وMeta Developer مجانيون جميعاً. فقط واتساب أعمال قد يحتاج توثيق رقم بمبلغ رمزي حسب سياسة Meta.

**كيف أنقل البيانات بين أجهزة الموظفين قبل الربط؟**
من لوحة الإدمن: تصدير نسخة JSON ← أرسلها للموظف ← يستوردها. (للتزامن التلقائي استخدم Supabase).
